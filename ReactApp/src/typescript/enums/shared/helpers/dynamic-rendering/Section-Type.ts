/**
 * @enum SectionType => Section Type enum representation. Usage in dynamic render method "Modules-Fetcher.tsx".
 */
enum SectionType  {
  Header = 0,
	Body = 1,
	Footer = 2
}

export default SectionType;