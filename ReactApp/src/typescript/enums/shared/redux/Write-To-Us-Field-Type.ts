/**
 * @enum WriteToUsFieldType => Additional enum type that determinates which field from write us form to update.
 */
enum WriteToUsFieldType {
  Message = "messageWriteUsField",
  Sender = "emailWriteUsField",
  Recipient = "recipientEmailField"
}

export default WriteToUsFieldType;
