/**
 * @type SocialIconModelType => Model type definition that represents social icon model item.
 * */
type SocialIconModelType = {
  code: string;
  name: string;
  url: string;
};

export default SocialIconModelType;
