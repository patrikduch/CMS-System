/**
 * @type ArticleModelType => Model type anotation that represents article`s entries.
 */
type ArticleModelType = {
  id: number;
  title: string;
  description: string;
  createdAt: string;
};

export default ArticleModelType;
