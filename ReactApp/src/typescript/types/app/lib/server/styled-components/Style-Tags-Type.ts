/**
 * @type StyleTagsType => Type anotation for manipulation with style atributte that has been generated via "Styled components" library.
 */
type StyleTagsType = {
  props: { dangerouslySetInnerHTML: { __html: string } };
};

export default StyleTagsType;
