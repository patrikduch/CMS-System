/**
 * @type AdminAuthStateType => Type anotation for Reselect selector that handels admin authentication process.
 */
type AdminAuthStateType = {
  adminlogin: {
    auth: false;
  };
};

export default AdminAuthStateType;
